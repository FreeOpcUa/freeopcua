Installation.

You can install and run server for testing purposes with using third party clients with next steps:

autoreconf -f -i
./configure --prefix=/path/to/server
make 
make install
cd /path/to/server
LD_LIBRARY_PATH=./lib ./bin/opcuaserver

