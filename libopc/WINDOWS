Prerequires.

1. Put libraries to:
   boost-(version) -> tools/boost
   cppunit-(version) -> tools/cppunit
   xulrunner-sdk-(version) -> tools/boost



Compiling.

1. Run Visual Studio Command Line.
2. run compiling with "nmake /f msvc.mk"
3. Run checks with "nmake /f msvc.mk check"


Clearing stuff.

1. Run Visual Studio Command Line.
3. Run cleaning with "nmake /f msvc.mk clean"
